# The dependency tree has a known peer conflict: openai@6 (root) vs
# @browserbasehq/stagehand@1.x (optional peer of @langchain/community)
# which requires openai@^4. Strict peer resolution refuses to install,
# which is why CI's `npm install` fails intermittently on main.
legacy-peer-deps=true
